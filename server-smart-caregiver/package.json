{
  "name": "fastapi-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy",
    "dev": "uv run pywrangler dev",
    "dev:uv": "uv run uvicorn src.main:app --reload",
    "dev:p": "uvicorn src.main:app --reload",
    "start": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  }
}
