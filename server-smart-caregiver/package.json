{
  "name": "fastapi-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy",
    "dev": "uv run pywrangler dev",
    "dev:local": "uvicorn src.worker:app --reload",
    "start": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  }
}
